% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seasonal_index_per_lake.R
\name{seasonal_index_per_lake}
\alias{seasonal_index_per_lake}
\title{Aggregate seasonal pixel indices to one lake-level index}
\usage{
seasonal_index_per_lake(seasonIndex, lakeName = NULL, lakeID = NULL)
}
\arguments{
\item{seasonIndex}{A list created by \code{\link[=seasonal_index_per_pixel]{seasonal_index_per_pixel()}} containing
at least \code{RSindex}, \code{QualityThreshold}, \code{x}, \code{y}, \code{crs}, and \code{year}.}

\item{lakeName}{Optional character scalar with the lake name. If \code{NULL}, the
output \code{Name} is \code{NA}.}

\item{lakeID}{Optional character scalar with a lake identifier. If \code{NULL}, an
identifier is generated by \code{\link[=add_lakeID]{add_lakeID()}} from mean coordinates.}
}
\value{
A list with index statistics (\code{IndexModusBest}, \code{IndexMedian},
\code{IndexSD}, \code{IndexModusPot}, \code{IndexDensity}, \code{IndexPixel}), metadata
(\code{QualityThreshold}, \code{x}, \code{y}, \code{crs}, \code{year}), and lake identifiers (\code{Name},
\code{ID}).
}
\description{
Summarizes the pixel-level seasonal index matrix of one lake and year into
lake-level statistics, including median, standard deviation, density-based
modal candidates, and a selected modal index value.
}
\details{
If more than one valid pixel is available, potential modes are
detected from a kernel density estimate using \code{bw = "SJ"} and \code{adjust = 2}.
Among sufficiently high local maxima, the mode with the highest density is
selected as \code{IndexModusBest}. If exactly one valid pixel is available, that
value is used for both median and mode.
}
