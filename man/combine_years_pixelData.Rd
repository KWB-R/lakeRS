% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_years.R
\name{combine_years_pixelData}
\alias{combine_years_pixelData}
\title{Combine annual lake index results into a harmonized pixel-wise data structure}
\usage{
combine_years_pixelData(lakeIndexList)
}
\arguments{
\item{lakeIndexList}{A list of yearly lake index lists created by
\code{\link[=seasonal_index_per_lake]{seasonal_index_per_lake()}}. Each element must contain (at minimum)
\code{IndexPixel}, \code{x}, \code{y}, \code{crs}, and \code{year} components.}
}
\value{
A list with the following elements:
\describe{
\item{\code{yearsIndex}}{A \code{data.frame} where each row corresponds to a pixel position
(\code{x}, \code{y}, \code{i_row}, \code{i_col}) and each \code{yYYYY} column stores the pixel values
of that year.}
\item{\code{crs}}{A character string giving the CRS of the final combined grid.}
}
}
\description{
This function merges multiple annual lake index maps (each produced by
\code{\link[=seasonal_index_per_lake]{seasonal_index_per_lake()}}) into a unified per-pixel dataset. It ensures that
all years are represented on a common spatial grid. If differences in CRS
or grid alignment between years exist, the rasters are resampled to match
the most recent year's configuration.
}
\details{
The function performs the following steps:
\enumerate{
\item Validates input to ensure \code{lakeIndexList} is a list of lists.
\item Extracts available years and their corresponding CRS values.
\item Selects the most recent year to define the target grid.
\item For each year, checks grid compatibility; if needed, resamples to the
target resolution and CRS using bilinear interpolation.
\item Merges all years’ pixel tables into a single data frame by coordinates.
}
}
\seealso{
\code{\link[=seasonal_index_per_lake]{seasonal_index_per_lake()}}, \code{\link[terra:resample]{terra::resample()}}, \code{\link[terra:rast]{terra::rast()}}
}
