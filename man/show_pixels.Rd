% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/show_pixels.R
\name{show_pixels}
\alias{show_pixels}
\title{Display pixel locations on an interactive map}
\usage{
show_pixels(x, y, crs, labels = NULL)
}
\arguments{
\item{x}{Numeric vector of x coordinates (e.g., easting or longitude in the
source coordinate reference system).}

\item{y}{Numeric vector of y coordinates (e.g., northing or latitude in the
source coordinate reference system). Must be the same length as \code{x}.}

\item{crs}{Character string specifying the source coordinate reference
system of \code{x} and \code{y}, in a format understood by
\code{terra::project()} (e.g., \code{"EPSG:32633"}).}

\item{labels}{Optional character vector of labels for the markers. If
provided, its length should match the length of \code{x} and \code{y}.}
}
\value{
A \code{leaflet} map object.
}
\description{
Projects input coordinates to WGS 84 and displays them as circle markers on a
leaflet map.
}
\details{
The marker colors are currently hard-coded as \code{c("blue", "green",
  "orange", "purple")}. If the number of points exceeds the number of colors,
colors will be recycled.
}
